spring:
  application:
    name: registry-server    # Très important : c'est ce nom qui lie au fichier Git
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: true
